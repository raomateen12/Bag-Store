<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Owner Registration - Scatch Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-purple-50 to-indigo-100 min-h-screen flex items-center justify-center">
    <div class="max-w-md w-full mx-4">
        <!-- Owner Registration Card -->
        <div class="bg-white rounded-lg shadow-xl p-8 border-t-4 border-purple-500">
            <!-- Logo & Title -->
            <div class="text-center mb-8">
                <div class="text-4xl mb-4"></div>
                <h1 class="text-3xl font-bold text-gray-800">Owner Registration</h1>
                <p class="text-gray-600 mt-2">Create admin account for Scatch store</p>
                <p class="text-xs text-orange-600 mt-1">(Development Mode Only)</p>
            </div>
            
            <!-- Error Messages -->
            <% if(error && error.length > 0) { %>
                <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-6">
                    <%= error %>
                </div>
            <% } %>
            
            <!-- Owner Registration Form -->
            <form action="/owners/register" method="POST" class="space-y-6">
                <!-- Full Name -->
                <div>
                    <label for="username" class="block text-sm font-medium text-gray-700 mb-2">Full Name</label>
                    <input type="text" 
                           id="username" 
                           name="username" 
                           required 
                           class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent"
                           placeholder="Enter your full name">
                </div>
                
                <!-- Email Field -->
                <div>
                    <label for="email" class="block text-sm font-medium text-gray-700 mb-2">Business Email</label>
                    <input type="email" 
                           id="email" 
                           name="email" 
                           required 
                           class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent"
                           placeholder="Enter business email">
                </div>
                
                <!-- Password Field -->
                <div>
                    <label for="password" class="block text-sm font-medium text-gray-700 mb-2">Password</label>
                    <input type="password" 
                           id="password" 
                           name="password" 
                           required 
                           minlength="8"
                           class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent"
                           placeholder="Create a strong password">
                    <p class="text-xs text-gray-500 mt-1">Password must be at least 8 characters long</p>
                </div>
                
                <!-- GSTIN Field -->
                <div>
                    <label for="gstin" class="block text-sm font-medium text-gray-700 mb-2">GSTIN Number</label>
                    <input type="text" 
                           id="gstin" 
                           name="gstin" 
                           required 
                           pattern="[0-9]{2}[A-Z]{5}[0-9]{4}[A-Z]{1}[1-9A-Z]{1}Z[0-9A-Z]{1}"
                           class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent"
                           placeholder="22AAAAA0000A1Z5">
                    <p class="text-xs text-gray-500 mt-1">Valid GSTIN format: 22AAAAA0000A1Z5</p>
                </div>
                
                <!-- Terms & Conditions -->
                <div class="flex items-start">
                    <input type="checkbox" 
                           id="terms" 
                           required 
                           class="mt-1 mr-2">
                    <label for="terms" class="text-sm text-gray-600">
                        I agree to the terms and conditions and confirm that I am authorized to create an admin account for this business.
                    </label>
                </div>
                
                <!-- Submit Button -->
                <button type="submit" 
                        class="w-full bg-purple-500 hover:bg-purple-600 text-white font-semibold py-3 px-4 rounded-md transition-colors">
                    Create Owner Account
                </button>
            </form>
            
            <!-- Login Link -->
            <div class="text-center mt-6">
                <p class="text-gray-600">
                    Already have an owner account? 
                    <a href="/owners/login" class="text-purple-500 hover:text-purple-700 font-medium">Login here</a>
                </p>
            </div>
        </div>
    </div>
</body>
</html>
