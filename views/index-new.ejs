<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch - Premium Bags Store</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-50">
    <!-- Header/Navbar -->
    <nav class="bg-white shadow-lg sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <a href="/" class="flex items-center">
                        <i class="fas fa-shopping-bag text-3xl text-blue-600 mr-2"></i>
                        <span class="font-bold text-xl text-gray-800">Scatch</span>
                    </a>
                </div>
                
                <div class="flex items-center space-x-4">
                    <a href="/" class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md font-medium">Home</a>
                    
                    <% if (isLoggedIn) { %>
                        <a href="/shop" class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md font-medium">Shop</a>
                        <a href="/cart" class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md font-medium relative">
                            <i class="fas fa-shopping-cart"></i>
                            <% if (user && user.cart && user.cart.length > 0) { %>
                                <span class="absolute -top-2 -right-2 bg-red-500 text-white text-xs rounded-full h-5 w-5 flex items-center justify-center"><%= user.cart.length %></span>
                            <% } %>
                        </a>
                        <span class="text-gray-700">Hi, <%= user.username %>!</span>
                        <a href="/logout" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-md font-medium">Logout</a>
                    <% } else { %>
                        <a href="/login" class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md font-medium">Login</a>
                        <a href="/register" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md font-medium">Register</a>
                    <% } %>
                    
                    <a href="/owners/login" class="text-sm text-gray-500 hover:text-gray-700">Admin</a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Flash Messages -->
    <% if (error && error.length > 0) { %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mx-4 mt-4">
            <% error.forEach(function(err) { %>
                <p><%= err %></p>
            <% }); %>
        </div>
    <% } %>

    <% if (success && success.length > 0) { %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mx-4 mt-4">
            <% success.forEach(function(msg) { %>
                <p><%= msg %></p>
            <% }); %>
        </div>
    <% } %>

    <!-- Hero Section -->
    <section class="bg-gradient-to-r from-blue-600 to-purple-700 text-white py-20">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid md:grid-cols-2 gap-12 items-center">
                <div>
                    <h1 class="text-5xl font-bold mb-6 leading-tight">
                        Premium Bags for Every Occasion
                    </h1>
                    <p class="text-xl mb-8 text-blue-100">
                        Discover our exquisite collection of handcrafted bags that combine style, functionality, and durability. Perfect for professionals, travelers, and fashion enthusiasts.
                    </p>
                    <div class="space-x-4">
                        <% if (isLoggedIn) { %>
                            <a href="/shop" class="bg-white text-blue-600 px-8 py-3 rounded-lg font-semibold hover:bg-blue-50 transition-colors">Shop Now</a>
                        <% } else { %>
                            <a href="/register" class="bg-white text-blue-600 px-8 py-3 rounded-lg font-semibold hover:bg-blue-50 transition-colors">Get Started</a>
                            <a href="/login" class="border-2 border-white text-white px-8 py-3 rounded-lg font-semibold hover:bg-white hover:text-blue-600 transition-colors">Login</a>
                        <% } %>
                    </div>
                </div>
                <div class="text-center">
                    <div class="relative">
                        <div class="bg-white/20 rounded-full p-8 inline-block">
                            <i class="fas fa-shopping-bag text-8xl text-white"></i>
                        </div>
                        <div class="absolute -top-4 -right-4 bg-yellow-400 text-yellow-900 rounded-full p-3">
                            <i class="fas fa-star text-2xl"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="py-16 bg-white">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
                <h2 class="text-3xl font-bold text-gray-900 mb-4">Why Choose Scatch?</h2>
                <p class="text-lg text-gray-600">Experience the perfect blend of quality, style, and functionality</p>
            </div>
            
            <div class="grid md:grid-cols-3 gap-8">
                <div class="text-center p-6">
                    <div class="bg-blue-100 rounded-full p-4 w-16 h-16 flex items-center justify-center mx-auto mb-4">
                        <i class="fas fa-gem text-2xl text-blue-600"></i>
                    </div>
                    <h3 class="text-xl font-semibold text-gray-900 mb-2">Premium Quality</h3>
                    <p class="text-gray-600">Crafted with the finest materials and attention to detail for long-lasting durability.</p>
                </div>
                
                <div class="text-center p-6">
                    <div class="bg-green-100 rounded-full p-4 w-16 h-16 flex items-center justify-center mx-auto mb-4">
                        <i class="fas fa-shipping-fast text-2xl text-green-600"></i>
                    </div>
                    <h3 class="text-xl font-semibold text-gray-900 mb-2">Fast Delivery</h3>
                    <p class="text-gray-600">Quick and secure delivery to your doorstep with real-time tracking.</p>
                </div>
                
                <div class="text-center p-6">
                    <div class="bg-purple-100 rounded-full p-4 w-16 h-16 flex items-center justify-center mx-auto mb-4">
                        <i class="fas fa-headset text-2xl text-purple-600"></i>
                    </div>
                    <h3 class="text-xl font-semibold text-gray-900 mb-2">24/7 Support</h3>
                    <p class="text-gray-600">Dedicated customer support team ready to help you anytime, anywhere.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Featured Products Section -->
    <section class="py-16 bg-gray-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
                <h2 class="text-3xl font-bold text-gray-900 mb-4">Featured Products</h2>
                <p class="text-lg text-gray-600">Discover our most popular bags loved by customers worldwide</p>
            </div>
            
            <% if (products && products.length > 0) { %>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
                    <% products.slice(0, 4).forEach(function(product) { %>
                        <div class="bg-white rounded-lg shadow-md hover:shadow-xl transition-shadow overflow-hidden">
                            <div class="<%= product.bgcolor %> h-48 flex items-center justify-center">
                                <div class="text-center">
                                    <i class="fas fa-shopping-bag text-4xl text-gray-600 mb-2"></i>
                                    <% if (product.image) { %>
                                        <img src="<%= product.image %>" alt="<%= product.name %>" class="w-full h-32 object-cover">
                                    <% } %>
                                </div>
                            </div>
                            <div class="p-6">
                                <h3 class="text-lg font-semibold text-gray-900 mb-2"><%= product.name %></h3>
                                <div class="flex items-center justify-between mb-4">
                                    <div>
                                        <span class="text-2xl font-bold text-gray-900">₹<%= product.Price - Math.round(product.Price * product.discount / 100) %></span>
                                        <% if (product.discount > 0) { %>
                                            <span class="text-sm text-gray-500 line-through ml-2">₹<%= product.Price %></span>
                                            <span class="text-sm text-green-600 ml-1">(<%= product.discount %>% off)</span>
                                        <% } %>
                                    </div>
                                </div>
                                <% if (isLoggedIn) { %>
                                    <a href="/addtocart/<%= product._id %>" class="w-full bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded-md text-center block font-medium transition-colors">
                                        Add to Cart
                                    </a>
                                <% } else { %>
                                    <a href="/login" class="w-full bg-gray-600 hover:bg-gray-700 text-white py-2 px-4 rounded-md text-center block font-medium transition-colors">
                                        Login to Buy
                                    </a>
                                <% } %>
                            </div>
                        </div>
                    <% }); %>
                </div>
                
                <% if (products.length > 4) { %>
                    <div class="text-center mt-12">
                        <% if (isLoggedIn) { %>
                            <a href="/shop" class="bg-blue-600 hover:bg-blue-700 text-white px-8 py-3 rounded-lg font-semibold transition-colors">
                                View All Products
                            </a>
                        <% } else { %>
                            <a href="/login" class="bg-blue-600 hover:bg-blue-700 text-white px-8 py-3 rounded-lg font-semibold transition-colors">
                                Login to See More
                            </a>
                        <% } %>
                    </div>
                <% } %>
            <% } else { %>
                <div class="text-center py-12">
                    <i class="fas fa-shopping-bag text-6xl text-gray-400 mb-4"></i>
                    <h3 class="text-xl font-semibold text-gray-600 mb-2">No Products Available</h3>
                    <p class="text-gray-500">Check back soon for new arrivals!</p>
                </div>
            <% } %>
        </div>
    </section>

    <!-- Call to Action Section -->
    <section class="py-16 bg-blue-600 text-white">
        <div class="max-w-4xl mx-auto text-center px-4 sm:px-6 lg:px-8">
            <h2 class="text-3xl font-bold mb-4">Ready to Find Your Perfect Bag?</h2>
            <p class="text-xl mb-8 text-blue-100">Join thousands of satisfied customers who trust Scatch for their bag needs.</p>
            <% if (isLoggedIn) { %>
                <a href="/shop" class="bg-white text-blue-600 px-8 py-3 rounded-lg font-semibold hover:bg-blue-50 transition-colors inline-block">
                    Start Shopping
                </a>
            <% } else { %>
                <div class="space-x-4">
                    <a href="/register" class="bg-white text-blue-600 px-8 py-3 rounded-lg font-semibold hover:bg-blue-50 transition-colors inline-block">
                        Sign Up Now
                    </a>
                    <a href="/login" class="border-2 border-white text-white px-8 py-3 rounded-lg font-semibold hover:bg-white hover:text-blue-600 transition-colors inline-block">
                        Login
                    </a>
                </div>
            <% } %>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white py-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid md:grid-cols-4 gap-8">
                <div>
                    <div class="flex items-center mb-4">
                        <i class="fas fa-shopping-bag text-2xl text-blue-400 mr-2"></i>
                        <span class="font-bold text-xl">Scatch</span>
                    </div>
                    <p class="text-gray-400">Premium bags for every occasion. Quality, style, and functionality combined.</p>
                </div>
                
                <div>
                    <h3 class="font-semibold text-lg mb-4">Quick Links</h3>
                    <ul class="space-y-2">
                        <li><a href="/" class="text-gray-400 hover:text-white">Home</a></li>
                        <li><a href="/shop" class="text-gray-400 hover:text-white">Shop</a></li>
                        <li><a href="/cart" class="text-gray-400 hover:text-white">Cart</a></li>
                    </ul>
                </div>
                
                <div>
                    <h3 class="font-semibold text-lg mb-4">Account</h3>
                    <ul class="space-y-2">
                        <% if (isLoggedIn) { %>
                            <li><a href="/logout" class="text-gray-400 hover:text-white">Logout</a></li>
                        <% } else { %>
                            <li><a href="/login" class="text-gray-400 hover:text-white">Login</a></li>
                            <li><a href="/register" class="text-gray-400 hover:text-white">Register</a></li>
                        <% } %>
                        <li><a href="/owners/login" class="text-gray-400 hover:text-white">Admin</a></li>
                    </ul>
                </div>
                
                <div>
                    <h3 class="font-semibold text-lg mb-4">Contact</h3>
                    <ul class="space-y-2 text-gray-400">
                        <li><i class="fas fa-phone mr-2"></i> 03097443097</li>
                        <li><i class="fas fa-envelope mr-2"></i> scatch@admin.com</li>
                        <li><i class="fas fa-map-marker-alt mr-2"></i> Pakistan, Lahore</li>
                    </ul>
                </div>
            </div>
            
            <div class="border-t border-gray-700 mt-8 pt-8 text-center">
                <p class="text-gray-400">&copy; 2025 Scatch. All rights reserved.</p>
            </div>
        </div>
    </footer>
</body>
</html>
